<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="sale_order_sinpvp_report" model="ir.actions.report">
            <field name="name">Presupuesto sinpvp</field>
            <field name="model">sale.order</field>
            <field name="report_type">qweb-pdf</field>
            <field name="paperformat_id">A4-integralia</field>
            <field name="report_name">sale.report_sale_order_sinpvp</field>
            <field name="print_report_name">(object.state in ('draft', 'sent') and 'Presupuesto - %s' % (object.name)) or 'Pedido - %s' % (object.name)</field>
            <field name="binding_model_id" ref="sale.order"/>
            <field name="binding_type">report</field>
            <field name="menu">"True"</field>
        </record>

    </data>
</odoo>
