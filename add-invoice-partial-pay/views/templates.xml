<?xml version='1.0' encoding='UTF-8'?>

<odoo>
    <data>
        <template id="report_invoice_document_partialpays"
            name="Factura con pagos a cuenta"
            inherit_id="report_invoice_document">
            <xpath expr="//span[@t-field='book.publisher_id']" position="after">
                <t t-if="not book.is_available">
                    <b>(Not Available)</b>
                </t>
	    </xpath>
        </template>

    </data>
</odoo>
